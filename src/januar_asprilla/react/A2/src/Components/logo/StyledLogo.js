import styled from "styled-components";

const LogoStyled = styled.figure`
  display: grid;
  justify-content: center;
`;
const ImageStyled = styled.img`
  max-width: 180px;
  cursor: pointer;
`;

export { LogoStyled, ImageStyled };
