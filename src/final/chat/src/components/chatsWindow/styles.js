import styled from "styled-components";

const StyledChatsWindow = styled.section`
  border-radius: 5px;
  grid-column: 3/4;
  grid-row: 2/3;
  overflow-y: auto;
`;

const StyledChatContainer = styled.div`
  width: 100%;
`;

export { StyledChatsWindow, StyledChatContainer };
